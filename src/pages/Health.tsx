import React from 'react';

const Health: React.FC = () => {
    return (
        <div className="flex items-center justify-center h-screen">
            <h1 className="text-2xl font-bold">Health Page</h1>
        </div>
    );
};

export default Health; 